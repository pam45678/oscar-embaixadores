services:
  - type: web
    name: oscar-embaixadores
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python seed_real.py"
    startCommand: "gunicorn app:app"
